listen 'github::push', repo:'<%= repo %><%= branch ? "##{branch}" : '' %>' do |repo_info|
  run 'heroku::deploy', app:'<%= app %>', content:repo_info.content do |deploy_info|
    success "Finished deploying '<%= repo %>' to '<%= app %>' with revision #{deploy_info.revision}"
  end
end